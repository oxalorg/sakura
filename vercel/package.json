{
  "name": "sakura-preview",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "npm run build:sakura && npm run build:vite",
    "build:vite": "vite build",
    "build:sakura": "cd .. && npm run build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^3.0.0"
  }
}
